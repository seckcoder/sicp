(import (generic-arithmetic)
        (init))

(init)
(display (=zero? (make-scheme-number 0)))(newline)
(display (=zero? (make-rational-number 0 4)))(newline)
(display (=zero? (make-complex-from-mag-ang 0 0)))(newline)
