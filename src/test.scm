(import (rnrs)
        (base)
        (complex)
        (init)
        (functional)
        (generic-arithmetic)
        (dict)
        )

(define dict (make-dict))
(display (dict 3 4))(newline)
(display (dict 3))

;(display (assoc 3 '((3 4) (4 5))))
