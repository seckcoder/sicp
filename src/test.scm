(import (rnrs)
        (base)
        (complex)
        (init)
        (functional)
        (generic-arithmetic)
        (dict)
        )

(init)
(define real (make-real-number 3.5))
(display (add real real))(newline)
